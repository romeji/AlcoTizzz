<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alcotizz.jlopes.com.alcotizz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity android:name=".activity_run">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_mode"/>
        <activity android:name=".activity_player"/>
        <activity android:name=".activity_partie"/>
        <activity android:name=".activity_player_coop"/>
        <activity android:name=".activity_partie_coop"
                  android:screenOrientation="landscape">

        </activity>
    </application>

</manifest>